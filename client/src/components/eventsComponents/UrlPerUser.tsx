import React from 'react'

const UrlPerUser: React.FC = () => {
    return (
        <div id="urlPerUser" style={{gridArea: '1 / 1 / 2 / 3'}}>
            url per user
        </div>
    );
}

export default UrlPerUser
